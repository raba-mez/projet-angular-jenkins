import { DragonSizePipe } from './dragon-size.pipe';

describe('DragonSizePipe', () => {
  it('create an instance', () => {
    const pipe = new DragonSizePipe();
    expect(pipe).toBeTruthy();
  });
});
